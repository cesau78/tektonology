- SPA: Journal: There's no comments per line, which is rather important for readability.
- SPA: Journal: The edit function doesnt seem to be working.
- SPA: Accounts: There should be a detail page for each account so that I can click into it and see all the transactions that affected the account.  I should also be able to click those transactions to go to a transaction details page.  The chart of account accounts should link to account details.  The Balance Sheet accounts should also link to account details. The Journal account should also link to account details.
- SPA: Components.  The Batch (aka print job), can produce multiple components of various types.  Instead of Cap, Slipper, there should be multiple lines with the same print job reference.
- The print jobs is too much for a single line - allow for a form style entry.  the product being printed, including github commit hash, should be on each design in the print job.  these items will transfer over to the components page if everything is succesful.

