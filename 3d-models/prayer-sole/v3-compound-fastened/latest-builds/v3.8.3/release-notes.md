# Prayer Sole v3.8.3 (compound fastened)

- Cap socket-pocket ribs: clamp outer +X to `outer_extent` so struts do not extend past the printed end face; shorter −X nib and tunable `boss_support_socket_*` in `config.scad`.
