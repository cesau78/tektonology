# Prayer Sole v3.9.0 (compound fastened)

- Cap/collar split: outer side-wall strips in the cap zone (+X of split) move from the collar to the cap — the material from each cap boss outward to the shell Y edge now prints on the cap piece.
- Cap print solidity: side bands overlap each boss by `boss_dia/2`, cap body uses full `cap_width`, and boss X placement aligns with the side-wall strip so the mesh fuses above/below the bosses (no slicer gaps).
