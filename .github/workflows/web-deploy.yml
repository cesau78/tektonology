name: Deploy Web to GitHub Pages

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

on:
  push:
    branches: [ "main" ]
    paths:
      - 'tektonology-spa/**'
      - '.github/workflows/web-deploy.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tektonology-spa/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: tektonology-spa

      - name: Run tests with coverage
        run: npm run test:coverage
        working-directory: tektonology-spa

      - name: Build
        run: npm run build
        working-directory: tektonology-spa

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: tektonology-spa/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
