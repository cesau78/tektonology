name: Kneeler Boot Compound Fastened STL Build

# Allow the workflow to push commits back to the repository
permissions:
  contents: write

on:
  push:
    branches: [ "main" ]
    paths:
      - '3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/*.scad'
      - '.github/workflows/kneeler-boot-compound-fastened-validation.yml'


jobs:
  validate-scad:
    runs-on: ubuntu-latest
    container:
      image: openscad/openscad:latest
    outputs:
      has_artifact: ${{ steps.check_artifacts.outputs.has_artifact }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Render Slipper
        run: |
          openscad -o 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-slipper.stl \
            3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-coupler.scad \
            -D 'piece="slipper"' -D 'preview=false' -D 'crosssection_view=false'

      - name: Render Cap
        run: |
          openscad -o 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-cap.stl \
            3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-coupler.scad \
            -D 'piece="cap"' -D 'preview=false' -D 'crosssection_view=false'

      - name: Render Insert
        run: |
          openscad -o 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-insert.stl \
            3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/kneeler-boot-insert.scad \
            -D 'preview=false' -D 'crosssection_view=false'

      - name: Upload STLs
        uses: actions/upload-artifact@v4
        with:
           name: kneeler-boot-compound-fastened-stls
           path: |
             3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/*.stl

      - name: Check for generated STLs
        id: check_artifacts
        run: |
          if ls 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/*.stl 1> /dev/null 2>&1; then
            echo "has_artifact=true" >> $GITHUB_OUTPUT
          else
            echo "has_artifact=false" >> $GITHUB_OUTPUT
          fi


  commit-stl:
    name: Commit Generated STL
    runs-on: ubuntu-latest
    needs: validate-scad
    if: needs.validate-scad.outputs.has_artifact == 'true'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Download generated STL artifact
        uses: actions/download-artifact@v4
        with:
          name: kneeler-boot-compound-fastened-stls
          path: kneeler-boot-compound-fastened-artifacts

      - name: Copy STL into repo
        run: |
          mkdir -p kneeler-boot-compound-fastened
          # move any stl produced into the repo directory (overwrite)
          mv -f kneeler-boot-compound-fastened-artifacts/*.stl 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/ || true

      - name: Commit and push generated STL
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 3d-models/kneeler-replacement-parts/kneeler-boot-compound-fastened/*.stl || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update generated STL [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          fi