{
  "name": "print-proxy",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "node --test proxy.test.js",
    "test:coverage": "node --test --experimental-test-coverage proxy.test.js",
    "generate-certs": "mkdir -p certs && openssl req -x509 -newkey rsa:2048 -keyout certs/server.key -out certs/server.crt -days 3650 -nodes -subj /CN=localhost"
  },
  "dependencies": {
    "aedes": "^0.51",
    "dotenv": "^16",
    "mqtt": "^5"
  }
}
