# Service-specific vars for print-proxy
# Shared vars (PRINTER_*, etc.) come from root .env

# Use a different port than 8883 when using netsh portproxy redirect,
# so the proxy can still reach the real printer on 8883 upstream.
PROXY_PORT=18883
